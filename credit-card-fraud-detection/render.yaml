services:
  - type: web
    name: fraud-detection-api
    runtime: python
    rootDir: credit-card-fraud-detection
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
