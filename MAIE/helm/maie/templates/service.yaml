apiVersion: v1
kind: Service
metadata:
  name: {{ include "maie.fullname" . }}
  labels:
    {{- include "maie.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "maie.selectorLabels" . | nindent 4 }}
